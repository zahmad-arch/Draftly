# AI drafting — get a key at https://vercel.com/dashboard → AI Gateway.
# Without it, the app runs in demo mode (templated sample proposals).
AI_GATEWAY_API_KEY=

# Payments — get keys at https://dashboard.stripe.com/apikeys (use test keys first).
# Without it, pricing buttons explain that checkout isn't live yet.
STRIPE_SECRET_KEY=
